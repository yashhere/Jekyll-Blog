  before-script:
    - gem install jemoji
    - gem install jekyll-paginate
    - gem install jekyll-gist
    - gem install jekyll
    
pages:
  stage: deploy
  image: ruby:2.3

    
  script:
    - jekyll build -d public/
  artifacts:
    paths:
      - public
  only:
    - master
